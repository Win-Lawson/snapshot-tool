i=1
function detect()
    if turtle.detect() then
        turtle.turnRight()
    end 
    if not turtle.detect() then
        turtle.forward()
    end
end
        

while 1<10 do
    turtle.forward()
    if 
    turtle.detect()
     
    then
        turtle.turnRight()
    end
    i=i+1
    turtle.forward()
end    
    
